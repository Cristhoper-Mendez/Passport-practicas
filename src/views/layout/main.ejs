<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    
    <% if (signupMessage) { %>
        <div>
            <h2><%= signupMessage %></h2>
        </div>
    <% } %>

    <% if (signinMessage) { %>
        <div>
            <h2><%= signinMessage %></h2>
        </div>
    <% } %>


    <%- body %>

</body>
</html>